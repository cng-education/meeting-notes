[workspace]
authors = ["Cloud Native Geospatial Education Working Group"]
channels = ["conda-forge"]
name = "meeting-notes"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
preview = "quarto preview"
render = "quarto render"

[dependencies]
quarto = ">=1.7.32,<2"
